# Based on a column after the File Review happens
# Any row with a value in the row[Column] = exclude should be dropped
# Save the dataframe to a new file in a new location as a csv


if __name__ == '__main__':
    run()